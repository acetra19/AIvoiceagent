# Kopieren auf dem Server: cp env.example .env
# N8N_HOST = exakt die Domain aus dem Caddyfile (ohne https://)

N8N_HOST=n8n.deinedomain.de

# Pflicht ab n8n 1.x für gespeicherte Credentials — einmal generieren und NIEMALS verlieren:
#   openssl rand -hex 32
N8N_ENCRYPTION_KEY=
